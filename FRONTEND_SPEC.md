# 구현·웹 감사 콘솔

2026-09-08 구현 기준. 사용자 통합 기준안 v1.0의 11~13절에 대응합니다. 실제 브라우저 렌더링·클릭 검증 완료 보고는 아닙니다.

| 영역 | 현재 구현 |
|---|---|
| 상단 | 서버 연결, CLI 실행 경로 설정 여부, 웹 확장 인증, 마지막 런 조회 시각 |
| 왼쪽 | 런 목록과 새 작업. 과거 조회와 동시 실행은 별개 |
| 시작 | 목표·ChatGPT 대화 URL, 서버 설정의 대상·요구사항 확인 |
| 현재 런 | 목표, 반복 회차, 현재 작업자, 단계·이유, 실제 상태 시각 |
| 접힌 개입 | 중단, 별도 적용, 감사 기록 다운로드. 비활성 이유 동반 |
| 감사 | 요구사항별 판정, 미해결·해결 지적과 처리 이력, 후보·실행 증거 원문 |
| 기록 | 서버가 보낸 이벤트·CLI/Web 메시지, 발생 시각 |

상단 신호는 관찰 사실을 그대로 이름 붙입니다. CLI 경로가 설정됐다는 사실을 실제 CLI 실행 성공으로 표시하지 않습니다. 요청 접수·외부 실행·감사 통과·적용을 분리합니다. 점수 입력, 저장소 경로 입력, 세션 발급, requestId, HTTP/WS 선택을 주 사용자 입력으로 노출하지 않습니다.

`HOLD`는 판단 보류, `INCONCLUSIVE`는 미해결 종료, `RECOVERY_REQUIRED`는 복구 확인 필요, `AWAITING_APPLY`는 감사 통과·적용 대기, `APPLIED`는 적용됨으로 표시합니다. 전체 완료·배포 성공으로 바꾸지 않습니다.

연결이 끊기면 마지막 확인 상태·시각을 유지하고 명령을 비활성화합니다. 늦은 이전 런 조회 응답은 현재 선택 런을 덮어쓰지 못합니다. 명령은 클릭 시 선택한 runId·expectedVersion을 고정합니다. 재렌더링 시 이벤트 발생 시각을 새 시각으로 바꾸지 않습니다. 알 수 없는 단계는 성공으로 정규화하지 않습니다.

증거 원문은 한 번에 200줄씩 조회하며 앞뒤 생략·전체 줄 수를 표시합니다. 증거와 지적은 textContent로 렌더링합니다. 에이전트가 보낸 HTML을 실행하지 않습니다. 감사 지적의 접힘 상태는 갱신 중 유지합니다.

색상은 bg `#0F1319`, panel `#161B24`, live `#4FD8A0`, warn `#E8A33D`, error `#E5484D`, info `#5B8DEF`입니다. Inter/UI와 IBM Plex Mono/데이터 폰트 스택을 사용하며 설치되지 않은 환경에서는 시스템 대체 폰트를 사용합니다. 외부 폰트 다운로드는 없습니다. 헤어라인 테두리와 760px 반응형 배치를 사용합니다.

`tests/audit-console.test.js`는 실제 UI 스크립트의 런 전환 경합·명령 대상·연결 끊김·시각 보존을 DOM 대역으로 검사합니다. `tests/helpers/console-preview.mjs`는 수동 화면 검사에만 사용하는 모의 서버이며 제품 서버에서 로드하지 않습니다. 실제 브라우저 사용 가능 시 이 화면의 초기 표시·시작·감사/기록 전환·원문 조회·적용·좁은 화면 배치를 추가 확인해야 합니다.

## 복구 확인 후 폐기

`RECOVERY_REQUIRED`이며 `run.abandon` capability가 있으면 외부 작업 종료 확인, 대상 상태 확인, 사유 입력을 제공한다. 세 항목을 모두 채워야 요청할 수 있다. 선택 런이 바뀌면 입력을 초기화한다. 요청은 해당 runId와 expectedVersion에 결합한다. 결과는 `CANCELLED`이며 기록 보존·적용 불가·새 런 가능을 구분한다. 서버가 외부 종료를 자동 확인했다는 문구는 사용하지 않는다.
