# 프론트엔드 기능·연결 계약 명세서

작성일: 2026-09-06

## 1. 문서 범위와 상태

현재 작업 트리의 프론트엔드와 대시보드 서버 구현을 기준으로, 화면이 받는 정보와 사용자가 실행할 수 있는 동작을 정리한다. 화면 구성은 사용자가 채택한 `dashboard-mockup.html`의 3열 배치를 기준으로 한다. 새로운 백엔드 기능을 정의하지 않는다.

초기 명세 작성 이후 사용자의 목업 적용 요청에 따라 프론트를 구현하고 이 문서를 갱신했다. 앞선 작업에서 추가한 자동 연결도 포함한다. 실행 중인 서버에 해당 변경이 반영되었는지, 브라우저에서 정상 동작하는지는 별도 확인 대상이다.

### 기준 소스

| 역할 | 기준 파일 |
|---|---|
| 실제 제공되는 화면 구조 | [public/index.html](public/index.html) |
| 화면 동작·API 호출·표시 조건 | [public/app.js](public/app.js) |
| 화면용 데이터 정규화·명령 포장 | [public/dashboard-model.js](public/dashboard-model.js) |
| HTTP 경로·인증·명령 응답 | [src/server.js](src/server.js) |
| 상태 응답·허용 명령·명령 실행 | [src/orchestration/dashboard-controller.js](src/orchestration/dashboard-controller.js) |
| 토론 시작 정책 | [src/domain/run-policy.js](src/domain/run-policy.js) |
| 실행 상태·종료 상태 | [src/domain/run-state-machine.js](src/domain/run-state-machine.js) |

서버는 `public/`을 제공한다. `currr/`는 검토에 사용한 프론트 파일이며 서버가 직접 제공하는 경로가 아니다. 화면 구성 전용 계약 문서나 스키마를 런타임에서 읽는 연결은 현재 없다.

## 2. 사용자 목적과 입력

사용자는 검토할 목표를 정하고 Codex와 ChatGPT의 토론을 시작한다. 진행 중인 응답과 결과를 확인하고, 필요하면 멈추거나 추가 지시를 전달한다. 과거 토론을 조회하고 기록을 다운로드할 수 있다.

| 입력 | 의미 | 현행 제약 |
|---|---|---|
| 목표 `objective` | 두 에이전트가 검토할 내용 | 공백 제거 후 비어 있으면 시작 불가 |
| 대화 주소 `conversationUrl` | 이번 토론에 사용할 기존 ChatGPT 대화 | 화면은 `https://chatgpt.com/c/...` 형식을 검사하고 서버가 대화 주소를 다시 검증 |
| 최대 응답 횟수 `maxTurns` | 토론의 최대 턴 수 | 서버 정책은 2 이상의 짝수 안전 정수. 화면 선택지는 2·4·6·8·12·20·50, 기본 선택은 6 |
| 추가 지시 `text` | 현재 Codex 턴에 전달할 내용 | 공백만 있는 값은 전송하지 않으며 활성 Codex 턴에서만 지원 |
| 조회할 토론 `runId` | 현재 화면에서 볼 토론 | 미지정하면 서버가 목록의 마지막 토론을 선택 |

모드는 `DISCUSSION`으로 고정되고 시작 에이전트는 Codex다. 서버 정책의 기본 최대 턴 수는 12지만, 현재 프론트는 선택된 값인 6을 기본 전송한다. 이 둘은 서로 다른 기본값이다.

토론에 사용할 ChatGPT 대화 탭과 브라우저 확장 연결이 필요하다. 대시보드 자동 연결이 ChatGPT 로그인·확장 설치·확장 인증·대화 선택을 대신하지는 않는다.

## 3. 화면에 제공되는 정보

아래 구분은 데이터의 의미를 설명한다. 탭·카드·사이드바 등 배치 방식은 지정하지 않는다.

| 정보 | 응답 필드 | 사용자에게 전달하는 의미 |
|---|---|---|
| 실행 준비 | `preflight`, `starting` | 시작 가능한지, 준비 중인지, 어떤 연결·설정이 부족한지 |
| 토론 목록 | `runs` | 조회할 토론의 식별자·목표·진행 상태 |
| 선택한 토론 | `run` | 목표·상태·응답 횟수·현재 에이전트·일시정지 여부 |
| 에이전트 세션 | `sessions` | Codex와 ChatGPT의 저장된 세션 정보 및 일부 현재 런타임 정보 |
| 대화 기록 | `messages` | 에이전트 출력과 컨트롤러가 전달한 입력 |
| 작성 중 응답 | `drafts` | 아직 저장되지 않은 에이전트별 응답 미리보기 |
| 결과 | `outcome`, `error` | 합의·합의 없이 종료·취소 등의 결과와 오류 |
| 사용 가능한 동작 | `commandCapabilities` | 서버가 현재 화면에 허용한다고 알리는 명령 목록 |
| 전달 내역 | `deliveries` | 메시지 전달 상태와 시도 정보 |
| 승인 내역 | `approvals` | 저장된 승인 관련 정보 |
| 이벤트 | `events` | 선택한 토론의 도메인 이벤트 기록 |

`run`은 토론이 없으면 `null`이다. 프론트 모델은 `runId`와 1 이상의 정수 `version`을 요구한다. 세션은 `CODEX_AGENT`, `CHATGPT_WEB_AGENT`별로 구분하며 같은 에이전트의 중복 세션은 정규화 오류다.

`runs`의 항목은 현재 서버에서 `runId`, `objective`, `phase`만 제공한다. 선택한 토론의 상세 정보는 `run`에서 가져온다.

`messages`에는 순수 대화문뿐 아니라 컨트롤러 입력 payload를 JSON 문자열로 만든 항목도 섞여 있다. 프론트는 선택한 토론의 최근 100개 항목을 단일 대화 피드로 표시한다. 두 항목의 생성 시각이 유효하고 다르면 시각순으로, 그 외에는 sequence와 원래 위치를 기준으로 정렬한다. 컨트롤러 전달 내용은 접힌 상세로 표시한다. 이 표시 순서를 서버의 새로운 인과 순서 계약으로 해석하지 않는다.

## 4. 사용자 동작과 명령

공통적으로 서버 연결이 확인되어야 하며, 화면은 명령 처리 중 중복 조작을 막는다. 버튼 활성화는 `commandCapabilities`와 프론트의 추가 조건을 함께 사용한다. 허용 목록은 화면 안내용이며 실제 실행 시 서버가 입력과 현재 상태를 다시 검증한다.

| 동작 | 명령 | 추가 입력 | 실행 조건·효과 |
|---|---|---|---|
| 토론 시작 | `run.start` | `mode`, `objective`, `conversationUrl`, `maxTurns` | 준비 검사 통과, 미완료 토론·기존 작업 없음. 세션을 준비하고 백그라운드 토론 시작 |
| 일시정지 | `run.pause` | 없음 | 비종료 토론에 대해 다음 전달을 멈추도록 요청. 현재 응답 중단과 별개 |
| 계속하기 | `run.resume` | 없음 | 서버가 재개 가능하다고 알린 토론. 현재 런타임의 일시정지 또는 복구 가능한 미전송 작업 재개 |
| 토론 종료 | `run.stop` | 없음 | 토론을 취소 상태로 전환하고 알려진 활성 턴이 있으면 중단 요청 |
| 현재 응답 중단 | `run.interrupt` | `actor`, `turnId` | 현재 에이전트·턴이 서버와 일치해야 함. 일시정지 후 해당 턴 중단 요청 |
| 추가 지시 | `run.steer` | `actor`, `turnId`, `text` | 활성 Codex 턴과 정확히 일치해야 함. ChatGPT 턴에는 지원하지 않음 |
| 대화 탭 열기 | `web.session.focus` | `sessionId` | 활성 턴·진행 작업이 없고 동일한 웹 세션 연결을 확인할 수 있어야 함 |
| 같은 대화 재연결 | `web.session.rebind` | `sessionId` | 동일한 웹 세션 연결 조건. 다른 대화로 교체하는 기능이 아님 |
| 기록 다운로드 | `evidence.export` | 없음 | 선택한 토론의 상태와 제안 기록을 비밀정보 제거 후 JSON으로 다운로드 |

토론 조회는 `GET /api/state?runId=...`를 사용한다. 서버는 `state.get` 명령도 지원하지만 현재 프론트의 주기적 조회는 GET 경로를 사용한다.

종료 버튼은 현재 프론트에서 같은 버튼을 4초 안에 다시 눌러 확인하는 방식이다. 이것은 프론트 조작 방식이며 서버의 별도 승인 계약은 아니다.

## 5. HTTP 연결 계약

### 5.1 자동 접속

`POST /api/dashboard/session`

- 서버에 `.env`의 `DASHBOARD_TOKEN`이 설정되어 있어야 한다.
- 요청의 로컬 접속 주소, 설정된 Host, 허용 Origin, `Sec-Fetch-Site: same-origin`을 확인한다.
- 성공 응답은 `{ "token": "..." }`이다. `.env`의 토큰 대신 서버 프로세스 수명 동안 유효한 별도 임시 토큰이다.
- 프론트는 토큰을 페이지 메모리에 보관하고 이후 요청의 Bearer 인증에 사용한다.
- 토큰 설정이 없으면 503, 로컬 브라우저 조건이 맞지 않으면 403을 반환한다.
- 직접 토큰을 입력하는 수동 연결 경로도 현재 남아 있다.

이 경로는 앞선 작업에서 추가된 구현이다. 변경 전 서버가 계속 실행 중이면 새 경로가 제공되지 않을 수 있다.

### 5.2 상태 조회

`GET /api/state` 또는 `GET /api/state?runId=<토론 ID>`

- Bearer 인증을 요구한다.
- 응답 필드는 3절에 정의한 현재 상태 묶음이다.
- 현재 프론트는 요청 완료 뒤 1.5초 후 다음 조회를 예약한다. 고정 주기 WebSocket 스트리밍이 아니다.
- 상태 조회 제한 시간은 15초다.
- 토론 선택 후 늦게 도착한 이전 조회 응답은 요청 순번과 선택 ID로 걸러낸다.
- 401 응답을 받으면 프론트는 접속 토큰을 비우고 다음 조회 시 자동 접속을 다시 시도한다.
- 런타임 준비 실패 등 상태 생성 오류는 503으로 반환될 수 있다. 상태 조회 자체도 런타임 생성 경로를 호출한다.

### 5.3 명령 요청

`POST /api/commands`

Bearer 인증과 허용 Origin을 요구한다. 공통 요청 구조는 다음과 같다.

```json
{
  "type": "run.pause",
  "requestId": "ui_example",
  "payload": {
    "runId": "선택한 토론 ID",
    "expectedVersion": 3
  }
}
```

기존 토론 명령은 조회한 `runId`와 `version`을 전송한다. 새 토론은 `runId` 없이 `expectedVersion: 0`을 전송한다. 버전이 달라지면 서버는 명령을 거부한다.

성공 응답은 `{ type: "command.result", requestId, payload }`다. 명령 실행 오류는 `{ type: "command.error", requestId, payload: { message, code } }`다. 인증 오류나 요청 형식 오류는 `{ error }` 형태일 수 있다.

같은 `requestId`에 같은 요청을 다시 보내면 서버가 보관 중인 결과를 재사용한다. 다른 요청 내용에 같은 ID를 사용하면 409를 반환한다. 이 중복 방지는 메모리 기반이며 서버 재시작을 넘어선 보장을 뜻하지 않는다.

프론트는 명령 응답을 받은 후 상태를 다시 조회한다. 시작 명령의 성공은 토론 전체의 완료가 아니라 세션 준비와 시작 요청 처리의 성공이다.

## 6. 상태·결과·오류 표현

| 상황 | 현행 의미 |
|---|---|
| 연결 확인 전·연결 실패 | 명령 비활성화, 오류 또는 연결 안내 표시 |
| 준비 조건 부족 | 시작 비활성화, `preflight`의 부족 항목 안내 |
| 미완료 토론 존재 | 새 토론 시작 불가. 진행 상태 조회·허용된 조작 가능 |
| 응답 진행 중 | 활성 에이전트와 세션 상태, 존재할 경우 draft 표시 |
| 일시정지 | `run.paused`로 표현. 종료 상태와 구분 |
| `COMPLETE` | 토론 완료. 합의 여부는 `outcome`을 함께 확인 |
| `FAILED` | 토론 실패 |
| `CANCELLED` | 토론 취소 |
| `CONSENSUS` 결과 | 두 에이전트의 합의 결과 |
| `INCONCLUSIVE` 결과 | 합의에 도달하지 못하고 종료한 결과 |
| 버전 충돌·다른 토론 진행 중 | HTTP 409, 현재 상태를 확인해야 함 |
| 일반 명령 거부 | HTTP 400과 오류 코드·설명 |

서버 재시작 뒤 남은 기록을 현재 연결된 실행으로 표시해서는 안 된다. 현재 서버는 안전하게 남은 미전송 작업에 한해 `run.resume`을 제공할 수 있다. 모든 과거 실행이 재개 가능하거나 반드시 종료해야 한다는 의미는 아니다.

프론트에서 표시하는 응답 경과 시간은 화면이 `RUNNING`을 관측한 시점부터 측정한다. 실제 서버의 턴 시작 시각이 아니며, 120초 초과 시 표시하는 ‘지연 확인’은 실패 판정이 아니다.

## 7. 확인된 계약 불일치와 미지원 기능

| 항목 | 현행 구현 | 화면 구성 검토 시 주의점 |
|---|---|---|
| 전달 재시도 | 대시보드 컨트롤러 미지원. 화면은 조회 안내만 제공 | 실행 버튼과 호출을 제거함 |
| 승인 결정 | 대시보드 컨트롤러 미지원. 화면은 승인 기록만 제공 | 실행 버튼과 호출을 제거함 |
| 목록 응답 횟수 | `runs`에는 상세 응답 횟수가 없음 | 목록에서는 상태만 표시하고 횟수를 추정하지 않음 |
| 재시작 안내 | 프론트는 오류 문자열의 ‘이전 서버 세션’ 포함 여부로 판단 | 구조화된 복구 상태 계약이 아니며 안내 문구에 의존함 |
| 복구 동작 | 서버는 조건부 재개를 제공 | 오른쪽의 계속하기·종료 버튼을 실제 capability에 따라 활성화 |
| 대화 내용 | 컨트롤러 입력 JSON과 에이전트 응답이 함께 표시됨 | 내부 전달 데이터가 사용자 대화와 같은 비중으로 노출될 수 있음 |
| 자동 연결 문구 | 자동 연결을 추가했지만 일부 초기·실패 안내는 수동 토큰 입력을 요구함 | 안내와 실제 연결 동작의 일관성 정리 필요 |

이 절은 확인된 차이를 기록한다. 미지원 명령의 신규 구현이나 기존 API 변경을 승인한 요구사항은 아니다.

## 8. 채택된 화면 구성

현재 계약으로 확인되는 것은 ‘주제를 입력하고 토론을 시작한다’, ‘선택한 토론의 대화와 결과를 본다’, ‘허용되는 진행 제어를 한다’, ‘과거 기록을 조회한다’는 기능이다.

사용자가 `dashboard-mockup.html` 구성을 존중하여 진행하도록 지시했다. 실제 화면은 다음 구성을 유지한다.

- 왼쪽: 연결 상태, 토론 목록, 새 토론 시작. 연결 상세는 접어서 제공.
- 가운데: 선택한 토론의 목표·상태, Codex와 ChatGPT의 단일 대화 피드, 작성 중 응답, 하단 추가 지시.
- 오른쪽: 진행 제어, 세션, 결과, 전달·승인·이벤트 진단.
- 새 토론 입력: 왼쪽 버튼에서 대화상자로 열고 목표·대화 URL·최대 응답 횟수를 입력.
- 좁은 화면: 영역을 순차 배치하고 각 동작에 접근할 수 있도록 반응형 처리.

목업의 정적 예시 대화와 상태는 실제 데이터로 교체한다. 목업 파일 자체는 참조로 보존하며 서버가 제공하는 `public/`에 구현한다. 화면 배치 채택은 미지원 백엔드 기능 추가를 의미하지 않는다.

## 9. 검증 범위

본 문서는 기준 소스의 현재 호출·응답·검증 로직을 대조해 작성했다. 화면의 시각적 적합성, 실제 브라우저 조작 성공, 실제 두 에이전트 토론 완료를 검증한 결과서는 아니다.

화면 변경 후 검증은 이 문서에 적힌 구현 사실과 해당 시점의 기준 소스를 다시 대조해야 한다. 특히 시작 응답과 최종 완료, 저장된 세션과 현재 연결, 미리보기와 저장 응답을 구별해야 한다.
